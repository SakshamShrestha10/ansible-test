---
- name: Copy files
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
  loop:
  - src: cv_web/
    dest: /var/www/site1/

  - src: site1
    dest: /etc/nginx/sites-available
  notify:
  - restart nginx
